services:
  backend:
    build: ./backend
    ports:
      - "8000:8080"
    env_file: .env
    environment:
      - PORT=8080

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_BASE=/api
    ports:
      - "5173:8080"
    depends_on:
      - backend
